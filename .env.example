# 拼音转换库环境变量配置

# 应用环境 (development, testing, production)
APP_ENV=production

# 调试模式 (true/false)
APP_DEBUG=false

# 字典根路径 (可选，默认为项目根目录下的 data 文件夹)
# PINYIN_DICT_ROOT_PATH=/path/to/your/dictionaries

# 跳过自动部署词库 (设置为 1 跳过)
# PINYIN_SKIP_AUTO_DEPLOY=0

# 生产环境标识 (自动跳过词库部署)
# CI=1
# COMPOSER_PROD_INSTALL=1